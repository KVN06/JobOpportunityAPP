package com.kvn.jobopportunityapp.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de colores profesional y seria - estilo Job Opportunity Web
val Primary = Color(0xFF1E3A8A) // Azul oscuro profesional
val PrimaryVariant = Color(0xFF1E40AF) // Azul medio
val Secondary = Color(0xFF374151) // Gris oscuro neutro
val SecondaryVariant = Color(0xFF4B5563) // Gris medio

val Background = Color(0xFFF9FAFB) // Blanco casi puro con tinte gris
val Surface = Color(0xFFFFFFFF) // Blanco puro
val SurfaceVariant = Color(0xFFF3F4F6) // Gris muy claro

// Color para la barra de estado - mantiene consistencia con el diseño
val StatusBarColor = Color(0xFFF9FAFB)

val OnPrimary = Color(0xFFFFFFFF)
val OnSecondary = Color(0xFFFFFFFF)
val OnBackground = Color(0xFF111827) // Negro carbón
val OnSurface = Color(0xFF1F2937) // Gris muy oscuro
val OnSurfaceVariant = Color(0xFF6B7280) // Gris medio

// Colores de estado más serios y profesionales
val Success = Color(0xFF065F46) // Verde oscuro profesional
val Warning = Color(0xFF92400E) // Naranja/marrón oscuro
val Error = Color(0xFF991B1B) // Rojo oscuro

// Colores de texto más contrastantes y legibles
val TextPrimary = Color(0xFF111827) // Negro carbón para máxima legibilidad
val TextSecondary = Color(0xFF6B7280) // Gris medio para texto secundario
val BorderColor = Color(0xFFD1D5DB) // Gris claro para bordes
val CardBackground = Color(0xFFFFFFFF) // Blanco puro para tarjetas

// Gradientes profesionales en tonos azul-gris
val GradientStart = Color(0xFF1E3A8A) // Azul oscuro
val GradientEnd = Color(0xFF374151) // Gris oscuro